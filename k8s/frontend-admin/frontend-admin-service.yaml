apiVersion: v1
kind: Service
metadata:
  name: frontend-admin
  namespace: ecommerce-hosekishops
spec:
  type: NodePort
  selector:
    app: frontend-admin
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30002
