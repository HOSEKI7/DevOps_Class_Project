name: CI/CD Kubernetes

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # ================= BACKEND =================
      - name: Build & Push Backend Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/backend-hosekishops:1.0 ./backend
          docker push ${{ secrets.DOCKER_USERNAME }}/backend-hosekishops:1.0

      # ================= FRONTEND STORE =================
      - name: Build & Push Frontend Store Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/frontend-store-hosekishops:1.0 ./frontend-store
          docker push ${{ secrets.DOCKER_USERNAME }}/frontend-store-hosekishops:1.0

      # ================= FRONTEND ADMIN =================
      - name: Build & Push Frontend Admin Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/frontend-admin-hosekishops:1.0 ./frontend-admin
          docker push ${{ secrets.DOCKER_USERNAME }}/frontend-admin-hosekishops:1.0

      # ================= KUBERNETES DEPLOY =================
      - name: Setup kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBE_CONFIG }}" > ~/.kube/config

      - name: Deploy to Kubernetes
        run: |
          kubectl set image deployment/backend backend=${{ secrets.DOCKER_USERNAME }}/backend-hosekishops:1.0
          kubectl set image deployment/frontend-store frontend-store=${{ secrets.DOCKER_USERNAME }}/frontend-store-hosekishops:1.0
          kubectl set image deployment/frontend-admin frontend-admin=${{ secrets.DOCKER_USERNAME }}/frontend-admin-hosekishops:1.0